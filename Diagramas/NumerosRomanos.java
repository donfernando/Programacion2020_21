import java.util.Scanner;

/**
 */
public class NumerosRomanos {

	// TODO Declare and initialise class variables here 

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Declare local variables here 
		
		// TODO Initialise local variables here 
		
		Leer de la entrada estandar;
		un numero entre 1 y 99;
		INPUT numero;
		dec = numero / 10 // DIV;
		unid = numero % 10 // MOD;
		switch (dec) {
		case 1:
			OUTPUT 'X';
			break;

		case 2:
			OUTPUT 'XX';
			break;

		case 3:
			OUTPUT 'XXX';
			break;

		case 4:
			OUTPUT 'XL';
			break;

		default:
			break;
		}
		if (unid == 1) {
			OUTPUT 'I';
		}
		else { if (unid == 2) {
			OUTPUT 'II';
		} 
		else { if (unid == 3) {
			OUTPUT 'III';
		}
		else { if (unid == 4) {
			OUTPUT 'IV';
		}
		else { if (unid == ...) {
			OUTPUT '....';
						}
					}
				}
			}
		}
	}

}
